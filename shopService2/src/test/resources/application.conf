ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }

    application {
        modules = [ ]
    }

    development = true

    config {
        name = "testName"
        value = "testValue"
    }
}